class Solution:
    def reverseWords(self, s: str) -> str:
        list=[]
        s=s.split()
        for word in s:
            list.append(word[::-1])
        return " ".join(list)

        
        
